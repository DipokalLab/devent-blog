<!DOCTYPE html>
<html lang="ko">
    {{> head}}
  <body>
    {{> navbar}}


    <header class="bg-white py-5">
        <div class="container px-5 pt-4 pb-2">
            <div class="row gx-5 justify-content-center">
                <div class="col-lg-6">
                    <div class="text-center my-5">
                        <h2 class="display-5 fw-bolder text-dark font-weight-lg mb-2">더 나은 <b class="font-weight-lg" id="blog_tags">제품을</b> 향해.</h2>
                        <p class="lead text-dark-50 font-weight-md mb-4" id="intro">데벤트 기술 블로그</p>
                        <div class="d-grid gap-3 d-sm-flex justify-content-sm-center" id="login_box">
                            <button class="btn btn-primary font-weight-md text-light btn-lg px-4 btn-rounded" onclick="location.href = '/auth/signup'"><i class="fas fa-user-plus"></i> 가입</button>
                            <button class="btn btn-outline-light font-weight-md text-light btn-lg px-4 me-sm-3 btn-rounded" onclick="location.href = '/auth/login'"><i class="fas fa-sign-in-alt"></i> 로그인</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </header>


    
    <section class="py-5" id="features">
        <div class="container-fluid px-5 my-5">
            <div class="row gx-5">
                <div class="col-lg-3">
                    <div class="feature bg-primary bg-gradient text-white rounded-3 mb-3"><i class="bi bi-collection"></i></div>
                    <h2 class="h4 fw-bolder">추천 태그</h2>
                    <p>Paragraph of text beneath the heading to explain the heading. We'll add onto it with another sentence and probably just keep going until we run out of words.</p>
                    <div class="d-grid gap-2">
                        <button class="btn color-blue-100 text-primary" onclick="appendFeeeeeeeeeeed()">피드 작성</button>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="feature bg-primary bg-gradient text-white rounded-3 mb-3"><i class="bi bi-collection"></i></div>
                    <h2 class="h4 fw-bolder">피드 </h2>




                    <pre id="logs" style="white-space: pre-wrap;">

                    </pre>
                </div>
                <div class="col-lg-3">
                    <div class="feature bg-primary bg-gradient text-white rounded-3 mb-3"><i class="bi bi-collection"></i></div>
                    <h2 class="h4 fw-bolder">Featured title</h2>
                    <p>Paragraph of text beneath the heading to explain the heading. We'll add onto it with another sentence and probably just keep going until we run out of words.</p>
                    <a class="text-decoration-none" href="#!">
                        Call to action
                        <i class="bi bi-arrow-right"></i>
                    </a>
                </div>
            </div>
        </div>
    </section>


 

    {{> footer}}

    <script>
        // Test Ground

        //getFeed(1)
        try {
            let token = getToken('user')
            if (checkToken(token)) {
                document.querySelector("#login_box").innerHTML = `<button class="btn btn-danger font-weight-md text-light btn-lg px-4 btn-rounded" onclick="logout()"><i class="fas fa-user-minus"></i> 로그아웃</button>`
                document.querySelector("#intro").innerText = '사용자로 로그인 완료'
            }
        } catch (error) {
            
        }

        let tag_title = ['세상을', '기술을', '내일을', '우리를', '오늘을', '생각을', '마음을', '지구를']
        let now_title = 0
        setInterval(() => {
            if (tag_title.length > now_title) {
                document.querySelector("#blog_tags").innerText = tag_title[now_title]
                now_title += 1
            } else {
                now_title = 0
                document.querySelector("#blog_tags").innerText = tag_title[now_title]

            }

        }, 2000)
    </script>

  </body>
</html>